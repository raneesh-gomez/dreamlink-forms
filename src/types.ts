export type DocTypeRow = {
    name: string;
    id: string;
    module: string;
};

export type UserRow = {
    username?: string;
    full_name?: string;
    email?: string;
    last_active: string;
};
